# 🔧 Очистка Service Worker для работы в Dev режиме

## Проблема

Service Worker перехватывает запросы к Vite dev server, что вызывает ошибки загрузки.

## Решение

### Способ 1: Автоматическая очистка (Рекомендуется)

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Application** (Приложение)
3. В левом меню найдите **Service Workers**
4. Найдите зарегистрированные Service Workers для `localhost:5173`
5. Нажмите **Unregister** для каждого
6. Перейдите в раздел **Cache Storage**
7. Удалите все кэши
8. Перезагрузите страницу (Ctrl+Shift+R)

### Способ 2: Через консоль

Откройте консоль браузера (F12 → Console) и выполните:

```javascript
// Отменить регистрацию всех Service Workers
navigator.serviceWorker.getRegistrations().then((registrations) => {
  registrations.forEach((registration) => {
    registration.unregister().then(() => {
      console.log('Service Worker unregistered');
    });
  });
});

// Очистить все кэши
caches.keys().then((cacheNames) => {
  cacheNames.forEach((cacheName) => {
    caches.delete(cacheName);
    console.log('Cache deleted:', cacheName);
  });
});

// Перезагрузить страницу
location.reload(true);
```

### Способ 3: Полная очистка данных сайта

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Application** (Приложение)
3. В левом меню найдите **Storage**
4. Нажмите **Clear site data** (Очистить данные сайта)
5. Выберите все опции и нажмите **Clear**

После очистки перезагрузите страницу.

