# 🌐 Кросс-платформенная поддержка

## Обзор

Панели YESS! GO теперь поддерживают работу на всех компьютерах и устройствах с улучшенной обработкой ошибок, совместимостью браузеров и надежностью.

## ✨ Реализованные улучшения

### 1. 🔍 Проверка совместимости браузера

Автоматическая проверка:
- Версии браузера
- Поддержки необходимых API
- Операционной системы
- Типа устройства (мобильный/планшет/десктоп)

**Файлы:**
- `shared/utils/browserCompatibility.ts`

**Функции:**
- `getBrowserInfo()` - определяет информацию о браузере
- `checkBrowserFeatures()` - проверяет поддержку необходимых функций
- `initBrowserCompatibility()` - инициализирует проверку при загрузке

### 2. 🔄 Улучшенная обработка ошибок

Централизованная обработка ошибок:
- Нормализация ошибок в стандартный формат
- Понятные сообщения для пользователей
- Автоматическое определение типа ошибки
- Логирование для отладки

**Файлы:**
- `shared/utils/errorHandler.ts`

**Функции:**
- `normalizeError()` - нормализует любую ошибку
- `getUserFriendlyMessage()` - получает понятное сообщение
- `shouldRedirectToLogin()` - проверяет необходимость редиректа

### 3. 🔁 Механизм повторных попыток (Retry)

Автоматические повторы запросов при ошибках:
- Умная логика повторных попыток
- Exponential backoff (экспоненциальная задержка)
- Определение повторяемых ошибок
- Настраиваемые параметры

**Файлы:**
- `shared/utils/retryUtils.ts`

**Особенности:**
- Повторяет только сетевые ошибки и 5xx ошибки сервера
- Не повторяет клиентские ошибки (4xx)
- Автоматическая задержка между попытками

### 4. 📡 Отслеживание онлайн статуса

Автоматическое определение подключения к интернету:
- Определение потери соединения
- Уведомления пользователя
- Автоматическое восстановление

### 5. ⚙️ Улучшенная конфигурация

**Vite конфигурация:**
- Поддержка ES2015+ (широкая совместимость)
- Увеличенные таймауты для медленных соединений
- Оптимизация сборки для разных платформ

**API конфигурация:**
- Увеличенный таймаут запросов (30 секунд)
- Улучшенная обработка ошибок сети
- Лучшие сообщения об ошибках

## 🖥️ Поддерживаемые платформы

### Операционные системы
- ✅ Windows (10, 11)
- ✅ macOS (10.15+)
- ✅ Linux (Ubuntu, Debian, Fedora и др.)
- ✅ Android (8.0+)
- ✅ iOS (14.0+)

### Браузеры
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ✅ Opera 76+

### Устройства
- ✅ Десктоп (большие экраны)
- ✅ Планшеты
- ✅ Мобильные телефоны

## 🚀 Использование

### Автоматическая инициализация

Проверка браузера и настройка отслеживания онлайн статуса выполняются автоматически при загрузке приложения в файлах:
- `partner-panel/src/main.tsx`
- `admin-panel/src/main.tsx`

### Обработка ошибок в компонентах

```typescript
import { getUserFriendlyMessage, logError } from '@/shared/utils/errorHandler';

try {
  // API запрос
  const data = await api.getData();
} catch (error) {
  // Логирование для разработчиков
  logError(error, 'MyComponent');
  
  // Показ понятного сообщения пользователю
  const message = getUserFriendlyMessage(error);
  message.error(message);
}
```

### Повторные попытки запросов

Механизм retry уже встроен в React Query и работает автоматически для всех запросов. Дополнительная настройка:

```typescript
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      retry: (failureCount, error) => {
        // Кастомная логика повторов
        return failureCount < 3;
      },
      retryDelay: (attemptIndex) => Math.min(1000 * 2 ** attemptIndex, 30000),
    },
  },
});
```

## ⚠️ Предупреждения

### Неподдерживаемые браузеры

Если браузер не поддерживается или отсутствуют необходимые функции, пользователь увидит предупреждение с рекомендацией обновить браузер.

### Офлайн режим

При потере подключения к интернету:
- Показывается предупреждение
- Запросы автоматически повторяются при восстановлении связи
- Кэшированные данные могут быть доступны

## 🔧 Настройка

### Переменные окружения

```env
# API URL (опционально, используется прокси по умолчанию)
VITE_API_URL=http://localhost:8000

# Режим разработки (автоматически определяется)
# VITE_ENV=development
```

### Таймауты

Таймауты можно настроить в:
- `partner-panel/src/services/partnerApi.ts` - `timeout: 30000`
- `admin-panel/src/services/adminApi.ts` - `timeout: 30000`

## 📊 Мониторинг

Все ошибки автоматически логируются в систему мониторинга:
- API ошибки
- Сетевые ошибки
- Ошибки валидации
- Ошибки авторизации

## 🐛 Отладка

### Проверка информации о браузере

В консоли браузера доступна информация:
```javascript
// В консоли браузера
console.log(browserInfo); // Информация о браузере
```

### Проверка онлайн статуса

```javascript
// Проверка статуса подключения
console.log(navigator.onLine); // true/false
```

## 📝 Примечания

- Предупреждения о браузере показываются только в production режиме
- В development режиме предупреждения не показываются для удобства разработки
- Все ошибки логируются в консоль для отладки
- Автоматические повторы запросов работают только для повторяемых ошибок

## 🔗 Связанные файлы

- `shared/utils/browserCompatibility.ts` - проверка браузера
- `shared/utils/errorHandler.ts` - обработка ошибок
- `shared/utils/retryUtils.ts` - механизм повторов
- `shared/monitoring/` - система мониторинга

