# 📊 Система мониторинга для YESS Panels

Полнофункциональная система мониторинга для отслеживания производительности, ошибок и метрик API в админ-панели и партнёр-панели.

## 🎯 Возможности

### 1. Мониторинг API запросов
- Отслеживание всех API запросов
- Статистика по статусам и endpoints
- Среднее время ответа
- История ошибок

### 2. Логирование ошибок
- Автоматическая обработка всех типов ошибок
- Группировка по типам и источникам
- История ошибок
- Фильтрация незначительных ошибок

### 3. Мониторинг производительности
- Web Vitals (FCP, LCP, FID, CLS, TTFB)
- Время загрузки страницы
- Время рендеринга компонентов
- Автоматическая оценка производительности

### 4. Дашборд метрик
- Отображение всех метрик в реальном времени
- Экспорт данных
- Автообновление

## 📦 Структура

```
monitoring/
├── apiMetrics.ts          # Метрики API запросов
├── errorLogger.ts         # Система логирования ошибок
├── performanceMonitor.ts  # Мониторинг производительности
├── index.ts              # Централизованный экспорт
└── README.md             # Этот файл
```

## 🚀 Быстрый старт

### 1. Импорт модулей

```typescript
import { 
  apiMetricsCollector,
  errorLogger,
  performanceMonitor,
  initializeMonitoring,
} from '../../../../shared/monitoring';
```

### 2. Инициализация

```typescript
// В App.tsx
useEffect(() => {
  initializeMonitoring();
}, []);
```

### 3. Интеграция в API клиенты

Подробные инструкции см. в `docs/MONITORING.md`.

## 📊 Использование

### Получение метрик API

```typescript
const summary = apiMetricsCollector.getRecentMetrics(5); // За последние 5 минут
console.log('Total requests:', summary.totalRequests);
console.log('Success rate:', summary.successfulRequests / summary.totalRequests);
```

### Логирование ошибок

```typescript
errorLogger.logApiError('/api/v1/users', 500, error);
errorLogger.logError({
  message: 'Something went wrong',
  source: 'api',
});
```

### Мониторинг производительности

```typescript
const metrics = performanceMonitor.getMetrics();
const score = performanceMonitor.getPerformanceScore(); // 'good' | 'needs-improvement' | 'poor'
```

### Полный отчет

```typescript
import { getMonitoringReport } from '../../../../shared/monitoring';

const report = getMonitoringReport();
console.log('API:', report.api);
console.log('Errors:', report.errors);
console.log('Performance:', report.performance);
```

## ⚙️ Конфигурация

Настройки через переменные окружения:

```env
VITE_ENABLE_METRICS=true
VITE_ENABLE_ERROR_LOGGING=true
VITE_ENABLE_PERFORMANCE_MONITORING=true
```

## 📚 Документация

Полная документация доступна в `docs/MONITORING.md`

## 📈 Метрики

### API Метрики
- Общее количество запросов
- Успешные/неудачные запросы
- Среднее время ответа
- Запросы по статусам
- Запросы по endpoints

### Ошибки
- Общее количество ошибок
- Ошибки по типам
- Ошибки по источникам
- Последние ошибки
- Критические ошибки

### Производительность
- FCP (First Contentful Paint)
- LCP (Largest Contentful Paint)
- FID (First Input Delay)
- CLS (Cumulative Layout Shift)
- TTFB (Time to First Byte)
- Время загрузки страницы

## 🔄 Экспорт данных

```typescript
import { exportMonitoringData } from '../../../../shared/monitoring';

const data = exportMonitoringData();
// Отправить на сервер или сохранить в файл
```

## ✅ Статус

- ✅ Система мониторинга API - **ГОТОВО**
- ✅ Система логирования ошибок - **ГОТОВО**
- ✅ Мониторинг производительности - **ГОТОВО**
- ✅ Документация - **ГОТОВО**

## 📝 Лицензия

Часть проекта YESS Backend.

